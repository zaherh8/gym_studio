<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta
      name="description"
      content="React Gym - Private personal training studio in Horsh Tabet, Lebanon. One-on-one sessions, certified trainers, flexible scheduling."
    />
    <meta
      name="keywords"
      content="personal training, gym, Lebanon, Horsh Tabet, Sin El Fil, private studio, fitness, React Gym"
    />
    <%!-- Open Graph --%>
    <meta property="og:title" content="React Gym — Your Personal Training Studio" />
    <meta
      property="og:description"
      content="React Gym - Private personal training studio in Horsh Tabet, Lebanon. One-on-one sessions, certified trainers, flexible scheduling."
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://gym-studio.fly.dev/" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:site_name" content="React Gym" />
    <%!-- Twitter Card --%>
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="React Gym — Your Personal Training Studio" />
    <meta
      name="twitter:description"
      content="React Gym - Private personal training studio in Horsh Tabet, Lebanon. One-on-one sessions, certified trainers, flexible scheduling."
    />
    <%!-- Additional SEO --%>
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://gym-studio.fly.dev/" />
    <%!-- Structured Data (JSON-LD) --%>
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "GymFitness",
        "name": "React Gym",
        "description": "Private personal training studio in Horsh Tabet, Lebanon. One-on-one sessions, certified trainers, flexible scheduling.",
        "url": "https://gym-studio.fly.dev/",
        "telephone": "+961 71 104 483",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Clover Park Bldg, 4th Floor, Horsh Tabet",
          "addressLocality": "Sin El Fil",
          "addressCountry": "LB"
        },
        "geo": {
          "@type": "GeoCoordinates",
          "latitude": 33.8773,
          "longitude": 35.5268
        }
      }
    </script>
    <.live_title default="React Gym" suffix=" · Your Personal Training Studio">
      {assigns[:page_title]}
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        
        window.addEventListener("phx:set-theme", (e) => setTheme(e.target.dataset.phxTheme));
      })();
    </script>
  </head>
  <body class="bg-base-100">
    <%!-- Modern Navigation Header --%>
    <header class="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b border-base-300/50 shadow-sm">
      <nav class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16 sm:h-20">
          <%!-- Logo --%>
          <a href={~p"/"} class="flex items-center gap-2 group">
            <span class="text-xl sm:text-2xl font-black">
              <span class="text-primary group-hover:text-primary/80 transition-colors">
                REACT
              </span>
              <span class="text-gray-900 group-hover:text-gray-700 transition-colors"> GYM</span>
            </span>
          </a>

          <%!-- Navigation Links --%>
          <div class="flex items-center gap-2 sm:gap-4">
            <%= if @current_scope do %>
              <span class="text-sm sm:text-base text-gray-600 hidden sm:inline">
                {@current_scope.user.name || @current_scope.user.email}
              </span>
              <%!-- Notification Bell --%>
              <.link
                href={
                  cond do
                    @current_scope.user.role == :admin -> ~p"/admin"
                    @current_scope.user.role == :trainer -> ~p"/trainer"
                    true -> ~p"/client/notifications"
                  end
                }
                class="btn btn-ghost btn-sm btn-circle relative text-gray-700 hover:text-primary"
              >
                <.icon name="hero-bell" class="size-5" />
                <% unread =
                  GymStudio.Notifications.count_unread_notifications(@current_scope.user.id) %>
                <%= if unread > 0 do %>
                  <span class="badge badge-error badge-xs absolute -top-1 -right-1 text-white text-[10px] min-w-[18px] h-[18px]">
                    {unread}
                  </span>
                <% end %>
              </.link>
              <.link
                href={~p"/users/settings"}
                class="btn btn-ghost btn-sm sm:btn-md rounded-full text-gray-700 hover:text-white hover:bg-gray-700"
              >
                Settings
              </.link>
              <.link
                href={~p"/users/log-out"}
                method="delete"
                class="btn btn-outline btn-sm sm:btn-md rounded-full text-gray-700 border-gray-300 hover:text-white hover:bg-primary hover:border-primary"
              >
                Log out
              </.link>
            <% else %>
              <.link
                href={~p"/users/log-in"}
                class="btn btn-ghost btn-sm sm:btn-md rounded-full text-gray-700 hover:text-white hover:bg-gray-700"
              >
                Log in
              </.link>
              <.link
                href={~p"/users/register"}
                class="btn btn-primary btn-sm sm:btn-md rounded-full shadow-md hover:shadow-lg transition-all"
              >
                Get Started
              </.link>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    {@inner_content}
  </body>
</html>
