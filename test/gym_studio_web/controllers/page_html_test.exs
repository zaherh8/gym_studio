defmodule GymStudioWeb.PageHTMLTest do
  use ExUnit.Case, async: true

  # format_operating_hours/1 was removed when we switched to static branches
  # (no operating hours displayed on the landing page).
  # If operating hours are re-introduced later, the function and tests
  # can be restored from git history (see PR for #93 + #96).
end
